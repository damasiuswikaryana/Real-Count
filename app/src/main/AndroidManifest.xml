<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.co.yakini.damasiusw.realcount">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".InputHasil" />
        <activity android:name=".VerifikasiInput" />
        <activity android:name=".InputSukses" />
        <activity android:name=".HasilData" />
        <activity android:name=".InputPenggugatan" />
        <activity android:name=".HasilDataGugatan" />
        <activity android:name=".VerifikasiInputGugatan" />
        <activity android:name=".BantuanActivity"></activity>
    </application>

</manifest>